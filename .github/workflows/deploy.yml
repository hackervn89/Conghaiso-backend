name: Build and Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy Backend
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Deploying Backend..."
            cd /var/www/conghaiso/conghaiso-backend
            
            echo "🔄 Pulling latest code..."
            git fetch --all
            git reset --hard origin/main

            echo "📦 Installing backend dependencies..."
            npm install --production

            echo "🔄 Reloading backend application with PM2..."
            # Sử dụng 'reload' để cập nhật không gián đoạn (zero-downtime)
            # Lệnh sau '||' sẽ tự động chạy nếu app chưa được start lần nào
            pm2 reload conghaiso-backend || pm2 start npm --name conghaiso-backend -- run start
            
            echo "✅ Backend deployed successfully."